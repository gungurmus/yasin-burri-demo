import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Rendert die Komponente <App /> welches das Frontend mit den JSX-Elementen enthält
/*-------------------------------------------------------------------------*/
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
